<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Attendance Dashboard | By Code Info</title>
  <link rel="stylesheet" href="css/pdash.css" />
  <!-- Font Awesome Cdn Link -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"/>
</head>
<body>
  <div class="container">
    


    <section class="main">
      <div class="main-top">
        <h1>Welcome's You !!</h1>
        <i class="fas fa-user-cog"></i>
      </div>
      <div class="users">
        <div class="card">
          <img src="<%= user.profile %>">
          <h4><%= user.username %></h4>
          
          
         
        </div>
        <div class="card">
          <h4>Age : <%= user.age %></h4>
          <h4>Blood Group : <%= user.blood_grp %></h4>
          <h4>Phone Number : <%= user.phone_number %></h4>
          <h4>Nominee Name : <%= user.nominee_name %></h4>
          <h4>Nominiee Relation : <%= user.nominee_relation %></h4>
        </div>
        <div class="card">
            <h4>BP : <%= user.age %></h4>
            <h4>Sugar Level : <%= user.sugar %></h4>
            <h4>Heart Rate: <%= user.heart_rate %></h4>
            <h4>Glucose: <%= user.glucose %></h4>
           
          </div>
        
      </div>
      <a href="updation.ejs">
      <button>EDIT PATIENT DETAILS</button></a>
      SEND MESSAGE TO NOMINEE
      <form action="/send_message" method="POST">
        <input type="text" name>
        <button typ="submit"></button>
      </form>
      

      <section class="attendance">
        <div class="attendance-list">
            <h1>Your Medical Records</h1>
            <table class="table">

                <tbody>
                    <form action="/upload" method="POST" enctype="multipart/form-data">
                        <input type="file" name="pdf" accept=".pdf">
                        <button type="submit">Upload PDF</button>
                    </form>
                    <% for (let i = 0; i < user.uploadedPDFs.length; i++) { %>
                        <tr>
                            <td><%= i + 1 %></td>
                            <td><%= user.uploadedPDFs[i].fileName %></td>
                            <td><a href="<%= user.uploadedPDFs[i].filePath %>" target="_blank"><button>View</button></a></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </section>
    
    </section>
  </div>

</body>
</html>
